# v0.5 Final Consistency Review Plan

Status: active internal v0.5 final consistency-review plan
Phase: 1.34
Manuscript files modified in this phase: no
Bibliography modified in this phase: no
Peer review package: not opened
Publication package: not opened
External circulation: not authorized

## Purpose

This document plans a final controlled consistency review of the current
`v0.5` manuscript state before any broader cleanup phase is opened. Its
purpose is to identify residual terminology drift, wording mismatches across
already patched sections, and citation or bibliography follow-up needs
without editing the manuscript in this phase.

This phase does not modify `paper.md`, `paper.tex`, or `references.bib`.

## Source-of-Truth Records

The following records control this plan:

- `v0.5-drafting-scope.md`
- `v0.5-manuscript-patch-plan.md`
- `v0.5-architecture-layer-patch-plan.md`
- `v0.5-contestability-layer-patch-plan.md`
- `v0.5-terminology-risk-plan.md`
- `v0.5-participation-readiness-patch-plan.md`
- `v0.5-evaluation-plan-patch-plan.md`
- `v0.5-drafting-entry.md`
- `v0.5-planning-record.md`
- `v0.5-governance-scope-plan.md`
- `author-decision-record-v0.5.md`
- `publication-readiness.md`
- `README.md`
- `../../governance/evidence-boundaries.md`
- `../../governance/public-disclosure-boundary.md`
- `../../research/terminology.md`
- `../../research/claims-register.md`

## Review Scope

The future consistency review should check:

- alignment among all sections already modified in the `v0.5` line;
- residual old manuscript-facing terms such as `Tenant`, `service nuclei`,
  `appeal`, `deployed`, `validated`, and `operational`;
- cross-section consistency between the architecture, limitations, research
  agenda, and conclusion;
- citation-key continuity after recent section-level rewrites; and
- whether any bibliography cleanup is needed later, without touching
  `references.bib` in this phase.

## Current Residual Consistency Findings

The current manuscript still contains residual older terminology and wording
in later sections outside the previously patched subsection windows.

### Residual manuscript-facing terminology still present

Current residual instances still appear in:

- `9. Limitations and Threats to Validity`
- `10. Research Agenda`
- `11. Conclusion`

Examples of residual drift that the later cleanup phase should review:

- `Tenant-level decisions`
- `synthetic Tenant-governance scenarios`
- `bounded Tenant autonomy`
- `service nuclei`
- `institutional appeals` or `appeal paths`

These do not necessarily violate repository policy in every use, but they are
now inconsistent with the manuscript-facing terminology decisions already
implemented in Sections `4.2`, `4.3`, and `4.6`.

### Residual wording that may deserve selective cleanup

The manuscript also retains some older terms that may still be acceptable in
bounded contexts but should be reviewed for consistency:

- `appeal`
- `deployed`
- `validated`
- `operational`

The future cleanup phase should distinguish between:

- acceptable negated or boundary-preserving uses; and
- inconsistent carryovers from earlier framing.

## Citation and Bibliography Status

Current citation-key status is clean:

- `paper.md` and `paper.tex` still use the same 12 citation keys;
- all 12 keys map to `references.bib`; and
- no orphaned bibliography entries were identified at this stage.

The currently cited keys are:

- `alfrink2023contestable`
- `amershi2019guidelines`
- `bai2022constitutional`
- `baxter2011sociotechnical`
- `hassan2021dao`
- `hsieh2018bitcoin`
- `kroll2017accountable`
- `leveson2012engineering`
- `schar2021defi`
- `tabassi2023airmf`
- `w3c2013provdm`
- `w3c2025vcdm`

The recent removal of a credential-oriented citation from Section `5` does not
yet create a bibliography cleanup action because `w3c2025vcdm` remains cited
elsewhere in the manuscript. Any later bibliography cleanup should be deferred
to a dedicated bibliography or readiness-cleanup phase.

## Future Cleanup Targets

The future controlled cleanup phase opened from this plan should focus on:

- replacing residual manuscript-facing `Tenant` usages outside Section `4.2`
  with `bounded local governance domain` phrasing where appropriate;
- replacing residual `service nuclei` usages outside Section `4.3` with
  `functional service modules` where appropriate;
- reviewing whether `appeal` should be softened to `challenge path` or
  `review condition` in later sections when the current wording sounds too
  operational;
- checking whether `deployed`, `validated`, and `operational` are used only in
  negated or boundary-preserving contexts; and
- preserving consistency between Section `8`, Section `9`, Section `10`, and
  Section `11` after the recent v0.5 patches.

## Forbidden Cleanup Actions in This Phase

This planning phase must not:

- edit the manuscript;
- edit `references.bib`;
- add or remove citations;
- weaken evidence or publication blockers;
- broaden the manuscript back toward product, platform, or operational
  language; or
- collapse boundary-preserving negative statements that still perform useful
  constraint work.

## Proposed Future Cleanup Shape

The future cleanup phase should:

- remain small and controlled;
- target only residual cross-section consistency drift;
- avoid new citations unless separately approved;
- avoid bibliography edits unless a separate bibliography cleanup is opened;
- keep Markdown and LaTeX synchronized; and
- produce a short cleanup report identifying which residual terms were kept,
  softened, or removed.

## Markdown/LaTeX Consistency Requirements

Any future cleanup patch opened from this plan must:

- update `paper.md` and `paper.tex` together;
- preserve semantic equivalence across all touched sections;
- avoid Markdown-only conceptual drift;
- avoid LaTeX-only conceptual drift;
- keep citation-key usage synchronized; and
- document any section-pair equivalence if paragraph structure differs.

## Verification Criteria for the Later Cleanup Phase

Before the later cleanup phase can be applied, require:

- confirmation of exactly which sections are in scope;
- confirmation that `references.bib` remains out of scope unless a separate
  bibliography cleanup is opened;
- confirmation that residual-term cleanup will not weaken boundary language;
- confirmation of a pairwise Markdown/LaTeX equivalence review; and
- confirmation of citation-key parity before and after cleanup.

## Verification Scans for the Later Cleanup Phase

Suggested scans:

- `rg 'Tenant|Tenants|service nuclei|service nucleus|Tenant-level|Tenant-governance' <changed-manuscript-hunks>`
- `rg 'appeal|appeals|appeal path|institutional appeals' <changed-manuscript-hunks>`
- `rg 'deployed|validated|operational' <changed-manuscript-hunks>`
- `rg '@[A-Za-z0-9:_-]+' papers/axodus-architecture-paper/paper.md`
- `rg '\\\\citep\\{[^}]+\\}' papers/axodus-architecture-paper/paper.tex`
- compare cited keys against `references.bib` only in a dedicated bibliography
  or readiness-cleanup phase
