# v0.6 Editorial Correction Plan

Phase: 2.8 follow-on planning
Status: bounded editorial correction plan
Date: 2026-07-12
Governing audit: `v0.6-editorial-consistency-audit.md`
Manuscript modified in this plan: no
Bibliography modified: no
Citations modified: no
Section 5 modified: no
Bibliography workstream: closed
Controlled external review: blocked
Publication: blocked
Submission: blocked
External circulation: blocked
Implementation authorized: executed in Phase 2.9 under separate human approval

## Purpose

This plan defines the smallest paired Markdown/LaTeX correction needed to
resolve findings `EC-01` through `EC-06` from the Phase `2.8` audit. It is a
plan only and does not modify manuscript or bibliography files.

## Phase 2.9 Implementation Record

Phase: 2.9
Status: editorial correction patch applied
Correction units applied: C-01 through C-05
Manuscript modified: yes
Bibliography modified: no
New citations added: no
Citations removed: no
Final citation-key count: 14
Bibliography workstream: closed
Section 5 conceptual scope: unchanged
Claims register: unchanged
Controlled external review: blocked
Publication: blocked
Submission: blocked
External circulation: blocked
Editorial closure audit: still required
v0.6 completion: not decided

| Unit | Findings | Affected files and surface | Resulting state | Citation impact | Claim impact | Boundary impact | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| C-01 | EC-01 | `paper.md` and `paper.tex`, front matter | Both identify an internal `v0.6` draft based on the closed `v0.5` baseline; all external gates remain blocked. | none | none | none | applied |
| C-02 | EC-02 | `paper.tex`, Section `2.3` | Malformed duplicate clause removed; sentence matches the Markdown proposition. | none | none | none | applied |
| C-03 | EC-03 | `paper.tex`, Section `2.1` | Technical-components and governance/accountability/review wording matches Markdown. | none | none | none | applied |
| C-04 | EC-04 | `paper.tex`, Section `4` list item 6 | Extra LaTeX-only state-transition clause removed. | none | none | none | applied |
| C-05 | EC-05; EC-06 | both References notes | Accurate repository-control wording is synchronized; LaTeX uses `\bibpreamble` under References. | none | none | none | applied |

Implementation resolves EC-01 through EC-06 on the authorized surfaces. Final
editorial closure remains subject to the short Phase `2.10` audit.

## Authorized Future Patch Surface

The future correction patch may modify only:

- `paper.md`; and
- `paper.tex`.

It must not modify `references.bib`, Section `5`, citations, claims records,
governance files, or unrelated manuscript paragraphs.

## Correction Map

| Unit | Findings | File and location | Smallest correction | Claim impact |
| --- | --- | --- | --- | --- |
| C-01 | EC-01 | both files, front matter | Align current version/status to internal `v0.6` and identify `v0.5` only as the closed prior baseline. | none |
| C-02 | EC-02 | `paper.tex`, Section `2.3` | Replace the malformed duplicated sentence with the existing Markdown-equivalent sentence. | none |
| C-03 | EC-03 | `paper.tex`, Section `2.1` | Align two bounded sentences to the primary Markdown wording. | none |
| C-04 | EC-04 | `paper.tex`, Section `4` list item 6 | Remove the LaTeX-only state-transition clause. | none; proposition remains in the preceding paragraph |
| C-05 | EC-05, EC-06 | both files, References note | Use an accurate shared verification-record description and place the LaTeX form under the generated References heading. | none |

## C-01 Front-Matter Status Alignment

In `paper.md`:

- change `version` from `Draft v0.5` to `Draft v0.6`; and
- replace the status with:

> Internal v0.6 academic draft based on the closed v0.5 internal baseline; not
> ready for controlled external review, external circulation, or publication

In `paper.tex`:

- change `\date{Draft v0.5}` to `\date{Draft v0.6}`; and
- use the same status sentence in the boxed status block.

Do not change title, author, affiliation, correspondence email, project forum,
author handle, license, document type, or keywords.

## C-02 Section 2.3 Sentence Repair

In `paper.tex`, replace only the malformed sentence beginning `These sources
motivate...` with the semantic equivalent already present in `paper.md`:

> These sources motivate an architecture in which AI outputs remain
> attributable, reviewable inputs to human decisions rather than sovereign
> decisions in themselves.

Do not alter either adjacent citation or the Constitutional AI paragraph.

## C-03 Section 2.1 Synchronization

In `paper.tex`, align the first paragraph of Section `2.1` with `paper.md` so
that it uses:

> A socio-technical perspective asks how roles, incentives, work practices,
> information, and technical components jointly shape outcomes.

and:

> Axodus adopts this perspective by modeling governance, accountability, and
> review as architectural elements rather than external policies attached
> after implementation.

Preserve the Baxter and Sommerville citation and all following text.

## C-04 Architecture List Synchronization

In `paper.tex`, change only Section `4` list item 6 from:

> risk-boundary and contestability controls constrain state transitions and
> keep challenge, correction, escalation, and review conditions available.

to the Markdown-equivalent wording:

> risk-boundary and contestability controls keep challenge, correction,
> escalation, and review conditions available.

Do not alter the preceding paragraph, where both formats already state that
risk boundaries constrain permissible state transitions.

## C-05 References Note Alignment

Use this equivalent note in both formats:

> References are maintained in `references.bib`. Verification records for all
> cited entries are maintained in the repository's bibliography control
> records; final metadata rechecking is required before any submission, public
> release, or external circulation.

In `paper.md`, retain the note under `# References` and replace only its stale
path-specific wording.

In `paper.tex`, remove the current note from the end of the Conclusion and use
natbib's bibliography-preamble mechanism so that the equivalent note appears
after the generated References heading and before bibliography entries. The
implementation phase must confirm the exact `\bibpreamble` syntax by compiling
the document; it must not hand-edit generated `.bbl` output.

This correction does not modify bibliography entries, citation keys, source
classification, or bibliography closure.

## Synchronization Requirements

The future patch must:

- preserve identical title, abstract, keywords, and section order;
- produce equivalent front-matter version and status language;
- preserve all 14 citation keys and their locations;
- preserve all 14 bibliography entries;
- leave Section `5` byte-for-byte unchanged;
- introduce no Markdown-only or LaTeX-only proposition;
- avoid unrelated line wrapping or prose cleanup; and
- keep all external-use gates blocked.

## Protected Surfaces

The future patch must not change:

- `references.bib`;
- any citation key or placement;
- Section `5` in either manuscript;
- title or abstract;
- author/contact metadata;
- `research/claims-register.md`;
- bibliography-control records except a later status pointer;
- root `README.md`;
- governance or disclosure files; or
- any private or operational content.

## Verification for the Future Patch

The implementation phase must run:

- pairwise front-matter comparison;
- section-by-section semantic comparison with zero substantive drift;
- heading and numbering extraction;
- 14/14/14 citation-key parity;
- orphan, undefined, and duplicate citation scans;
- duplicate-passage scan;
- residual-term and boundary scans;
- exact Section `5` comparison against the pre-patch baseline;
- protected-file diff checks;
- `git diff --check`; and
- a fresh `latexmk -g -pdf paper.tex` build with BibTeX validation.

Any bibliography, citation, Section `5`, claim, or external-authorization diff
must block completion.

## Proposed Next Phase

Phase `2.10` is complete with `PASS_CLOSED`. The next recommended workstream is
**Institutional Phase 2.11 - v0.6 External-Readiness Preparation Plan**.

Phase `2.11` may define readiness criteria and review public-facing metadata
and disclosure boundaries. It must not reopen bibliography expansion, perform
broad copyediting, close `v0.6`, or authorize controlled external review,
publication, submission, or external circulation.

## Phase 2.10 Closure Verification

Phase: 2.10
Status: editorial closure audit completed
Decision: `PASS_CLOSED`
Correction units verified: C-01 through C-05
Findings verified resolved: EC-01 through EC-06
Manuscript modified during closure audit: no
Bibliography modified: no
Final citation graph: 14/14/14
Section 5: unchanged
Claims register: unchanged
Controlled external review: blocked
Publication: blocked
Submission: blocked
External circulation: blocked
v0.6 completion: not decided

`v0.6-editorial-closure-audit.md` confirms every correction unit was applied
as planned and identifies no implementation defect. The associated
`v0.6-editorial-workstream-closure-record.md` closes only the editorial-
consistency workstream.

This plan remains the traceable source for the original correction authority.
No further correction is authorized by the closure, and reopening requires a
new explicit editorial decision.
